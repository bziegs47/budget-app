{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for budget windows",
  "windows": ["main", "budget-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
